apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "system-info-api.fullname" . }}-test-connection"
  labels:
    {{- include "system-info-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "system-info-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
